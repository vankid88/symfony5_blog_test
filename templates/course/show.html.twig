{% extends 'layout/default.html.twig' %}

{% block pageTitle %}Course detail {% endblock %}

{% block title  %}Course detail{% endblock %}

{% block breadcrumb %}Course detail{% endblock %}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ course.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ course.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-sm btn-default" href="{{ path('course_index') }}"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back to list</a>
    <a class="btn btn-sm btn-warning text-light" href="{{ path('course_edit', {'id': course.id}) }}"><i class="fa fa-pencil-square" aria-hidden="true"></i> Edit</a>

    {{ include('course/_delete_form.html.twig') }}
{% endblock %}
